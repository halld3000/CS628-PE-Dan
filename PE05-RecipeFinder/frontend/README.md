The following program uses react and the useState hook to manage the recipe finder. AddRecipe.js, RecipeDetails.js, and Recipelist.js pages pass data to backend database. Reciipelist has functions to add and delete task, it displays the input field and add task button. Captures user input and sends it back to App.js. Renders a list of Recipes compnents dynamically.
